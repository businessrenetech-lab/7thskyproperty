export const metadata = {
  title: "Branch Student Booking | Language Academy",
  alternates: {
    canonical: "/student-booking",
  },
  robots: {
    index: false,
    follow: false,
  },
};

export default function StudentBookingLayout({ children }) {
  return <>{children}</>;
}
