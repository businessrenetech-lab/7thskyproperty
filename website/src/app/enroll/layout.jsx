export const metadata = {
  title: "Course Enrollment & Payment | Language Academy",
  alternates: {
    canonical: "/enroll",
  },
  robots: {
    index: false,
    follow: false,
  },
};

export default function EnrollLayout({ children }) {
  return <>{children}</>;
}
