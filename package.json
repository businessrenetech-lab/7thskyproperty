{
  "name": "seventh-sky-properties",
  "version": "1.0.0",
  "description": "Seventh Sky Properties — Backend API + Admin Portal + Website",
  "main": "production-server.js",
  "scripts": {
    "start": "node production-server.js",
    "build:admin": "cd admin-portal && npm install --include=dev && npx vite build",
    "build:website": "cd website && npm install --include=dev && npx next build --webpack",
    "build:all": "npm run build:admin && npm run build:website",
    "postinstall": "node scripts/postinstall.js"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3"
  },
  "overrides": {
    "postcss": "8.5.10"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
